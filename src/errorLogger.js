/**
 * Generate fallback error component
 * @param {string} error error message
 * @param {string} errorInfo error details
 */
export default ({ error, errorInfo }) => {
  console.log(error, errorInfo);
};
